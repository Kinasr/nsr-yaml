date-config:
  date-pattern: "yyyy-MM-dd"
  time-pattern: "HH:mm:ss"
  date-time-pattern: "yyyy-MM-dd HH:mm:ss"
  zoned-date-time-pattern: "yyyy-MM-dd HH:mm:ss Z"

# The first environment can be configured dynamically using the command-line property 'NSR_ENV'.
environments:
  - local
  - test_server
